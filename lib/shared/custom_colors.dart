import 'package:flutter/material.dart';

/// A class that defines custom color constants.
class CustomColors {
  /// Represents the primary red color.
  static const Color red = Color(0xFFE65541);

  /// Represents the standard white color.
  static const Color white = Color(0xFFFFFFFF);

  /// Represents a light gray color
  static const Color lightGray = Color(0xFFF7F7F7);

  /// Represents a darker gray color
  static const Color darkGray = Color(0xFFEBEBEB);

  /// Represents a strong opacity black color.
  static const Color strong = Color(0xCC000000);

  /// Represents a medium opacity black color.
  static const Color medium = Color(0x66000000);

  /// Represents a light opacity black color.
  static const Color light = Color(0x33000000);

  /// Represents a fully transparent color.
  static const Color transparent = Color(0x00000000);
}
